// Autogenerated from java.template file
class {{entity.name}} {

  {% for attribute in entity.attributes %}
  protected {{attribute.type|javatype}} {{attribute.name}};
  {% endfor %}

  {% for attribute in entity.attributes %}
  public {{attribute.type|javatype}} get_{{attribute.name}}(){
    return this.{{attribute.name}};
  }

  public void set_{{attribute.name}}({{attribute.type|javatype}} new_value){
    this.{{attribute.name}} = new_value;
  }
  {% endfor %}

}
